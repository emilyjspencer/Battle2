Players successfully created, <%= @game.name(1) %> vs. <%= @game.name(2) %>

<%= @game.name(2) %> hp = <%= @game.player_hp(2) %>

<form action = "/attack" method= "POST">
<input type="submit" value="attack">
</form>
